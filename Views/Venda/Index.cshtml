@{
    ViewData["Title"] = "Vendas";
}

@model List<SistemaVenda.Entidades.Venda>;

<h2 class="mb-3 text-primary">Vendas</h2>
<hr />

<div class="table-responsive">
    <table class="table table-striped table-hover table-bordered shadow">
        <thead class="table-dark">
            <tr>
                <th class="text-center">Código</th>
                <th>Data</th>
                <th>Cliente</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr onclick="Editar(@item.Codigo)" style="cursor: pointer;">
                    <td class="text-center fw-bold">@item.Codigo</td>
                    <td>@item.Data</td>
                    <td>@item.CodigoCliente</td>
                    <td>@item.Total</td>
                </tr>
            }
        </tbody>
    </table>
</div>

<br />

<button type="button" class="btn btn-primary btn-lg shadow rounded" onclick="Cadastrar()">Registrar Venda</button>

<script>
    function Cadastrar(){
        window.location = window.origin + "\\Venda\\Cadastro";
    }

    function Editar(Codigo){
        window.location = window.origin + "\\Venda\\Cadastro\\" + Codigo;
    }
</script>
